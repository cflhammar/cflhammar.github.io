import React, {FC, useEffect, useState} from "react";
import sof1 from "./sof52.png";

const SofImage:FC = () => {
    return (
                <img src={sof1} style={{ width: "30%", height: "auto", margin: "auto", "marginRight": 0, paddingRight:"30px"}}/>
    );
};

export default SofImage;